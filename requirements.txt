torch>=2.12.1
pydantic>=2.13.4
transformers
datasets
tokenizers
deepspeed
pyyaml
xxhash
tensorboard
safetensors

# Dev / testing
pytest>=9.1.1
pytest-xdist

# Optional — install manually for full functionality
# flash-attn>=2.6          # FlashAttention-3 (H100/H200 recommended)
# wandb                    # Experiment tracking
# lm-eval                  # Benchmark harness (scripts/eval_benchmarks.py)
# torchao                  # FP8 training (fp8_training: true in config)
